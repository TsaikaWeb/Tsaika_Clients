@model IEnumerable<Tsaika_Clients.Models.Clients>

@{
    ViewBag.Title = "Admin";
}

<h2>Admin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Eng_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gender)
        </th>
        <th></th>
    </tr>

@foreach (var x in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => x.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => x.Eng_Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => x.Gender)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = x.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = x.Id })
    </td>
</tr>
}

</table>
